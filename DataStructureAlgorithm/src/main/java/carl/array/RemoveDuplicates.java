package carl.array;

/**
 * @description: LC.26 删除有序数组中的重复项
 * @author: HammerRay
 * @date: 2024/5/13 下午10:06
 */
public class RemoveDuplicates {
    public int removeDuplicates(int[] nums) {
        if (nums == null || nums.length == 0) {return 0;}
        int slow = 0 ;
        for (int fast = 0; fast < nums.length; fast++) {

        }
        return slow;
    }
}
